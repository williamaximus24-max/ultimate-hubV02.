-- =============================================================================
-- NIGHT COMMUNITY | TELA DE CARREGAMENTO + LOADER PRINCIPAL INTEGRADO
-- =============================================================================

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

-- Evita duplicar a tela se o script for executado duas vezes
if CoreGui:FindFirstChild("NightHubLoading") then
    CoreGui.NightHubLoading:Destroy()
end

-- Criação da Interface Principal de Loading
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NightHubLoading"
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

if syn and syn.protect_gui then
    syn.protect_gui(ScreenGui)
    ScreenGui.Parent = CoreGui
elseif gethui then
    ScreenGui.Parent = gethui()
else
    ScreenGui.Parent = CoreGui
end

local Background = Instance.new("Frame")
Background.Name = "Background"
Background.BackgroundColor3 = Color3.fromRGB(10, 5, 20)
Background.Size = UDim2.new(1, 0, 1, 0)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.BorderSizePixel = 0
Background.Parent = ScreenGui

local CenterContainer = Instance.new("Frame")
CenterContainer.Name = "CenterContainer"
CenterContainer.BackgroundTransparency = 1
CenterContainer.Size = UDim2.new(0, 600, 0, 400)
CenterContainer.AnchorPoint = Vector2.new(0.5, 0.5)
CenterContainer.Position = UDim2.new(0.5, 0, 0.5, 0)
CenterContainer.Parent = Background

for i = 4, 1, -1 do
    local circle = Instance.new("Frame")
    circle.Name = "Wave"
    circle.AnchorPoint = Vector2.new(0.5, 0.5)
    circle.BackgroundColor3 = Color3.fromRGB(40, 10, 70)
    circle.BackgroundTransparency = 0.8 + (i * 0.03)
    circle.Size = UDim2.new(0, i * 130, 0, i * 130)
    circle.Position = UDim2.new(0.5, 0, 0.5, 0)
    circle.Parent = CenterContainer
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = circle
end

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "Title"
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0.5, 0, 0.15, 0)
TitleLabel.AnchorPoint = Vector2.new(0.5, 0.5)
TitleLabel.Size = UDim2.new(0, 500, 0, 60)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "Night Hub"
TitleLabel.TextColor3 = Color3.fromRGB(160, 100, 255)
TitleLabel.TextScaled = true
TitleLabel.Parent = CenterContainer

local SubtitleLabel = Instance.new("TextLabel")
SubtitleLabel.Name = "Subtitle"
SubtitleLabel.BackgroundTransparency = 1
SubtitleLabel.Position = UDim2.new(0.5, 0, 0.32, 0)
SubtitleLabel.AnchorPoint = Vector2.new(0.5, 0.5)
SubtitleLabel.Size = UDim2.new(0, 400, 0, 30)
SubtitleLabel.Font = Enum.Font.GothamMedium
SubtitleLabel.Text = "Loading Night Hub..."
SubtitleLabel.TextColor3 = Color3.fromRGB(180, 175, 195)
SubtitleLabel.TextSize = 22
SubtitleLabel.Parent = CenterContainer

local BarBackground = Instance.new("Frame")
BarBackground.Name = "BarBackground"
BarBackground.BackgroundColor3 = Color3.fromRGB(30, 15, 50)
BarBackground.AnchorPoint = Vector2.new(0.5, 0.5)
BarBackground.Position = UDim2.new(0.5, 0, 0.48, 0)
BarBackground.Size = UDim2.new(0, 450, 0, 20)
BarBackground.Parent = CenterContainer

local BarBgCorner = Instance.new("UICorner")
BarBgCorner.CornerRadius = UDim.new(1, 0)
BarBgCorner.Parent = BarBackground

local ProgressBar = Instance.new("Frame")
ProgressBar.Name = "ProgressBar"
ProgressBar.BackgroundColor3 = Color3.fromRGB(130, 50, 255)
ProgressBar.Size = UDim2.new(0, 0, 1, 0)... (39 KB restante(s))
